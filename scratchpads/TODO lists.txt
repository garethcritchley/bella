TODO lists

genral:
    connect dynabyte < and >
    rename github  project
    reconnect to new worker
    remove DNS from domains
    add bellasetc.com to worker
    add cname @ / www
    add routes to the worker *.bellasetc.com/*
    potentially add bulk redirect
    test
        
Services:

index:


puppers:
    fix puppers images heights
    fix tri fold pupper issue


taken from critical css, card and big card '.services' bordering for cards
    border-top: 0.375em solid var(--primary);
    border-left: 0.375em solid var(--primary);
    border-right: 0.375em solid var(--primary);
    border-bottom: 0.375em solid var(--primary);

